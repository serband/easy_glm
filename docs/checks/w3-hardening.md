# W3 — workbench hardening: the break-it findings, replayed

*Generated by `scripts/checks/w3_hardening.py` on a synthetic 3,000-policy book (text policy numbers, a 0/1 train flag, three rating factors). The thirteen rows are the blocking findings of `docs/reviews/w2-breakage.md`: 1–4 lost work, 5–13 were tracebacks. "Now" is what this run of the tool actually said.*

| # | What the tester did | What used to happen | What happens now |
|---|---|---|---|
| 1 | New empty project, then type a name | The new project inherited the old file's path and the next autosave emptied the old project file. | The first click asks you to click again; the new project starts with no file (path shown: None), and typing a name changes nothing on disk — the old file is byte-for-byte unchanged: yes. |
| 2 | The same project open in two tabs, both editing | Each tab's autosave wrote its whole copy over the other tab's work; the last tab to touch anything won silently. | Tab B's autosave is paused with the notice “<project file> was changed by another browser tab or session since this tab last saved it. Autosave is paused…”. The file still holds tab A's version (name 'edited-by-tab-A'); after *Reload from disk* tab B shows 'edited-by-tab-A' and its own stale edit is dropped. |
| 3 | Rename the target column in the roles grid | The Target box silently jumped to the first column (the policy number) and that was autosaved. | Renaming ClaimNb to claims carries the role and the model's target with it (target is now 'claims'). A model whose column has truly gone is refused with: “target column 'claims' is not in the data”. |
| 4 | Random split with the split column named ClaimNb (the target) | The 0/1 flag replaced the target column; the fit 'succeeded' on nonsense. | Refused: “The random split column 'ClaimNb' would overwrite an existing data column; choose another name on the Split page”. The Split page shows the same message and keeps the old name. |
| 5 | Load a data file whose columns differ from the project's | Traceback on Variables and Split; the project was unusable until the file was edited by hand. | Every page renders (0 tracebacks across Variables, Split, Model); the Variables page lists the roles whose columns are gone and the Model page says “freq: predictor(s) not in the data: ['DrivAge', 'Region']”. E.g. “Columns with a role that are not in this data file: **DrivAge** (predictor), **Region** (predictor)…”. |
| 6 | Rename BonusMalus to DrivAge (a name already in use) | Traceback 'column DrivAge is duplicate'; the rename was saved and the role lost. | Refused, nothing saved (changed=False): “Cannot rename 'BonusMalus' to 'DrivAge': another column already has that name. Rename not saved.”. |
| 7 | Clear a 'rename to' cell with Delete | Traceback ('float' has no attribute 'strip'); the rename and the lost role stayed in the file. | An emptied cell means 'no rename': renames are now {}, BonusMalus is back as a 'predictor' and the model predicts with ['DrivAge', 'BonusMalus', 'Region']. |
| 8, 9 | Add a derived column that refers to itself, or divides a text column | Traceback on Variables and Split; the column was saved and every page that needs the data was broken. | *Add* runs the expression first and refuses: “pl.col('foo') + 1 fails: unable to find column "foo"; valid columns: …” / “pl.col('Region') / 2 fails: division with 'String' datatypes is not a…”. A derived column that breaks later is reported as a data-step problem on every page instead of a traceback. |
| 10 | Split page with no usable split column; a text column as indicator with TRAIN = 1 | Traceback 'cannot compare string with numeric' because the page picked the first column (the policy number) by itself. | Nothing is picked for you: 0 tracebacks, the page says “The split column 'nope' is not in the data. Pick the indicator column above or …”; a text column compares as text and “No row of 'Region' equals the TRAIN value '1'; check the value on the Split page”. |
| 11 | Upload or open five kinds of broken project file | Tracebacks (JSON decode errors, key errors) and in one case a half-loaded project replaced the open one. | All five end with a message starting “Not a valid easy_glm project” (yes); the open project is left alone every time (yes). E.g. “Not a valid easy_glm project: truncated.json is not valid JSON (Expecting property name e…”. |
| 12 | Save to a folder that cannot exist, to '/', or autosave to a read-only file | Tracebacks (PermissionError, FileNotFoundError, FileExistsError); a failed autosave was only mentioned on the Project page. | Messages: “Could not save the project to /nonexistent_dir_easy_glm/x.json: [Errn…”, “/ is a folder; give the project file a name”. The project keeps its previous file (still w3check.easyglm-project.json). A failing autosave is shown at the top of every page: “Autosave failed: [Errno 13] Permission denied: '/var/folder…”. |
| 13 | Create a model called a/b, then open Rate tables or Export | Traceback from the Excel writer (the name became a folder in the file path); the whole Rate tables and Export pages were dead. | The Create button is disabled with “Model name cannot contain '/'”; names are trimmed, limited to 60 characters and must be unique. A legacy project that already holds such a name still downloads: its files are called a_b.xlsx / .easyglm. |

## The two rules behind the fixes

**One project file, many tabs.** The workbench remembers when it last read or wrote the project file. Before every autosave it looks at the file again; if someone else (another tab, another session, a text editor) has changed it since, autosave pauses and a notice at the top of every page offers two choices: *Reload from disk* (this tab takes the other version and drops its own unsaved edits, including anything still typed into its boxes) or *Overwrite* (this tab's version wins). Nothing is written until you choose, so neither copy can be lost by accident. Two people should still not edit one project file at the same time — the rule stops silent loss, it does not merge.

**A model may only use columns that exist.** Renaming a column in the roles grid renames it everywhere it is used (role, type, recode, design, split, every model's target / weight / offset / predictors / constraints / interactions / adjustments). It follows the column into your row filters and derived-column formulas too: a filter written as `pl.col('Exposure') > 0.02` becomes `pl.col('exposure_years') > 0.02`, and the page tells you which formulas were rewritten (only that column reference is touched — the rest of the formula is left exactly as you typed it). Changing a column's role tells you which models are affected. If a column a model needs is missing from the data (a new file, a removed derived column), the model is not silently re-pointed at another column: the Model page shows which column is missing, its selector is left blank, Fit is disabled and any persisted fit is ignored until you choose a replacement.

## What to check yourself

- Open your own project in two tabs, change something in each, and confirm the second tab shows the notice rather than a merged or overwritten file.
- Rename a rating factor in the roles grid, then look at the Model page: the predictor list, any monotone constraint and any interaction should show the new name, and a fitted model should say it needs refitting.
- Type a nonsense expression into a derived column and a slash into a model name: both should be refused with a sentence, never a traceback.

## Findings not fixed in W3

Items 15 (a role change now lists the models it touched, but still applies immediately), 23 (roles are still keyed by the final column name; renames now propagate correctly) and 35 (the 'pick two different variables' message shows in every Diagnostics tab) are cosmetic and left for a later piece. Item 32 is fixed: the Seed box now always shows the seed the project holds, so the page can no longer name a seed the split did not come from. Items 28 (a fit interrupted by a page refresh) and 38 (a constant column blocking the fit) were closed in the next piece — see `docs/checks/w4-runs-folder.md`, which also covers what the second breaker session found.
